{
  "name": "@churchos/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn app.main:app --reload --port 8000",
    "build": "echo 'API: no build step'",
    "test": "pytest",
    "lint": "ruff check .",
    "type-check": "mypy app/"
  }
}
