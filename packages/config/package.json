{
  "name": "@churchos/config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./tailwind": "./tailwind.config.ts",
    "./tokens.css": "./src/tokens.css",
    "./components.css": "./src/components.css"
  },
  "scripts": {
    "build": "echo 'config: no build step'",
    "lint": "echo 'config: no lint'",
    "type-check": "tsc --noEmit",
    "test": "echo 'config: no tests — tokens are validated through UI component tests'"
  },
  "peerDependencies": {
    "tailwindcss": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
